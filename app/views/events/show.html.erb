<% if @user_events[@user_events.index(@event) + 1] %>
  <%= link_to "Next Event", "/events/#{@user_events[@user_events.index(@event) + 1].id}", class: "next" %>
<% end %>
<%= render partial: "event_info", locals: {event: @event} %>

<script id="event-template" type="text/x-handlebars-template">

  <h1>{{name}}</h1>
  <h3>{{date}}</h3>
  <h3>{{cost}}</h3>
  <h3>{{location}}</h3>
  <h3>{{description}}</h3>

</script>

<br></br>

<%= link_to 'Edit Event', edit_event_path %> <br></br>

<%= button_to 'Delete Event', event_path, method: :delete %><br></br>
